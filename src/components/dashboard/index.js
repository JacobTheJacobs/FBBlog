import React from "react";
import Layout from "../../utils/layout";

const Dahsboard = (props) => {
  return (
    <Layout auth={props.auth} title="Dashboard">
      dasd
    </Layout>
  );
};

export default Dahsboard;
